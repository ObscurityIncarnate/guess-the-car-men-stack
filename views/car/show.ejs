<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title><%= car.name %></title>
</head>
<body>
    <%- include("../../partials/nav.ejs") %>
    <h1><%= car.name %></h1>
    <p><%=car.manufacturer%></p>
    <p><%=car.manufacture_date%></p>
    <p><%=car.description%></p>
    <div style="width: 360px; height: 300px;">
        <img src="<%= car.image %>" alt="<%= car.name %>" style="width: 100%; height: 100%;">
    </div>
    <form action="/cars/car._id?_method=DELETE" method="POST">
        <button type="submit">
            Remove Car
        </button>
    </form>
    <a href="/cars/<%=car._id%>/edit">
        <button type="submit">
            Update Details
        </button>
    </a>
</body>
</html>